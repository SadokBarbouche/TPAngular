import { ArccielDirective } from './arcciel.directive';

describe('ArccielDirective', () => {
  it('should create an instance', () => {
    const directive = new ArccielDirective();
    expect(directive).toBeTruthy();
  });
});
